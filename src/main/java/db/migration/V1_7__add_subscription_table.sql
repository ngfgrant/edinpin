CREATE TABLE `edinpin`.`subscription` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '',
  `userid` INT NOT NULL COMMENT '',
  `resourcetypeid` INT NOT NULL COMMENT '',
  PRIMARY KEY (`id`)  COMMENT '');